* how to change the curve style?
  - window.style(curveid,style=...) 
  - curve.style(style=...);  window.replot(curveid)

* how to use defaults?
  - always use default for API (choose settings for all packages)
  - always use default for package (use each package's default setttings)
  - allow user to choose

