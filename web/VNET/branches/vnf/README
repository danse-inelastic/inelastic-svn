To deploy:

create a directory "vnf" in <cgi-bin>
copy the following to <cgi-bin>/vnf
  - main.sh
  - dottools
create sym links to 
  - config
  - content
  - sampleassemblies
create sym link to "html" and call it "vnf" in html root, eg, /var/www.


install postgre (https://help.ubuntu.com/community/PostgreSQL)
add a database "vnf"
add a role (user name) to postgre. It should be the same as
the user name of your apache server.

